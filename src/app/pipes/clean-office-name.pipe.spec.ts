import { CleanOfficeNamePipe } from './clean-office-name.pipe';

describe('CleanOfficeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanOfficeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
